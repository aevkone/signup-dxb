---
import View from '@/views/ProductionCategory.astro';
import { CATEGORY_SLUGS } from '@/data/production';

export const getStaticPaths = () => CATEGORY_SLUGS.map((slug) => ({ params: { slug } }));
---

<View slug={Astro.params.slug!} />
