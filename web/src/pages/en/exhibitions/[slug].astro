---
import View from '@/views/ExhibitionBlock.astro';
import { BLOCK_SLUGS } from '@/data/exhibitions';

export const getStaticPaths = () => BLOCK_SLUGS.map((slug) => ({ params: { slug } }));
---

<View slug={Astro.params.slug!} />
